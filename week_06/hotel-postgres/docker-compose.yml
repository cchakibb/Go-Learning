services:
  postgres:
    image: postgres:16
    environment:
      - POSTGRES_USER=hotel
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=hoteldb
    ports:
      - "5433:5432"
    volumes:
      - my_volume:/var/lib/postgresql/data

volumes:
  my_volume: